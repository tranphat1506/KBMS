sequenceDiagram
    participant E as EntryNode
    participant A as AlphaNode (x)
    participant B as BetaNode (Join)
    participant T as TerminalNode

    E->>A: ReceiveToken (Fact: x=10)
    A->>A: Condition Check (x > 5?)
    A->>B: ReceiveRight (Token: [x=10])
    Note over B: Store in RightMemory
    Note over B: Join with LeftMemory
    B-->>T: Propagate (Token: [y=20, x=10])
    T->>T: Trigger OnActivation
